#import "template.typ": *

= Typink ノート

これはテストノートです。

連続の式：
$ (partial rho)/(partial t) + div (rho vf(u)) = 0 $

非圧縮性流体：
$ div vf(u) = 0 $

#figure(
  image("figures/test.svg"),
  caption: [手書き図のプレースホルダ]
)
